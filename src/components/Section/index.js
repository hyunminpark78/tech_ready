import { default as Section } from './Section.vue';

export default Section;

import { default as SectionLnb } from './SectionLnb.vue';
import { default as SectionHeader } from './SectionHeader.vue';
import { default as SectionNewHeader } from './SectionNewHeader.vue';
import { default as SectionMain } from './SectionMain.vue';

export { SectionLnb, SectionHeader, SectionNewHeader, SectionMain };
